import React from 'react';

const Score = (props) => {
        return (<div className="score">
                    Score
                    <div>{ props.score }</div>
                    </div>);
}
export default Score;